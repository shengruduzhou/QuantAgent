[project]
name = "quantagent"
version = "0.4.1"
description = "Governed A-share quantitative research, backtesting, and paper-trading platform."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26,<2.3",
  "pandas>=2.2,<3",
  "polars>=1.0",
  "pyarrow>=16.0",
  "scipy>=1.13",
  "pydantic>=2.7",
  "pyyaml>=6.0",
  "typer>=0.12",
  "tabulate>=0.9",
  # Unconditional production import in quantagent.data.ashare.http, the shared
  # paced/retrying transport every public A-share adapter uses. It is core, not
  # optional: without it the data foundation cannot import at all. Omitting it
  # here is what broke CI collection in a clean environment while passing on
  # developer machines that had it installed transitively.
  "requests>=2.31,<3",
]

[project.optional-dependencies]
data = [
  "akshare",
  "tushare",
]
training = [
  "torch>=2.3",
  "transformers>=4.43",
  "scikit-learn>=1.5",
]
research = [
  "pyqlib>=0.9.6",
  "vectorbt>=0.26",
]
optimization = [
  "cvxpy>=1.4",
]
web = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "httpx>=0.27",
]
test = [
  "pytest>=8.2",
  "pytest-cov>=5.0",
  "scikit-learn>=1.5",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "httpx>=0.27",
]
all = [
  "akshare",
  "tushare",
  "torch>=2.3",
  "transformers>=4.43",
  "scikit-learn>=1.5",
  "pyqlib>=0.9.6",
  "vectorbt>=0.26",
  "cvxpy>=1.4",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "httpx>=0.27",
]

[project.scripts]
quantagent = "quantagent.cli:app"

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
