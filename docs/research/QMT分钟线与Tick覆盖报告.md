# QMT 分钟线与 Tick 覆盖报告

- **生成时间**：2026-07-28（UTC）
- **源提交**：`9787de0727d2caa8c3720891f1af8b4af9b4017d`

## 1. 结论

**未测量。** 本机为 Linux，无 Wine／xtquant／MiniQMT；QMT 为 Windows 客户端。 未取得任何 QMT 分钟线或 tick 数据。

因此本报告不给出任何覆盖率、历史长度或标的数字。

## 2. 与日线主线的关系（重要）

按任务要求的正确次序，**分钟／tick／Level-2 是独立赛道，不得阻塞日线主线**：

```
CI 绿 → 恢复 PR → QMT 权限探测 → ST PIT 闭环 → 严格数据证书
      → 全宇宙 Gold → 有界冒烟训练 → 全量日线训练
      → （可选）分钟/tick/Level-2 研究
```

Gold 桥已实现 QMT 可得性掩码（`has_tick_events` 等），且**近期 QMT tick 覆盖
不是日线基线的前置条件**。

## 3. 已有的公开 tick 事实（非 QMT，来自本仓库既有实测）

作为对照，本仓库此前已实测的公开源结论仍然成立：

- 腾讯"分笔"是 **3 秒聚合**，不是逐笔（间隔恰为 3.0s；`amount ≠ price×volume`
  占 48%–82%），分类为 `SNAPSHOT_DERIVED_TRADE_AGGREGATE`；
- 新浪历史分笔接口**已下线**（HTTP 200 + `服务已下线`）；
- 公开源 **Level-2 全族零供应商**。

详见 `docs/research/A股Tick与Level2供应商矩阵.md`。

## 4. QMT 就绪后需测量的项

| 项 | 方法 |
| --- | --- |
| 1m 实际最早日 | 请求 19900101，记录实际首行 |
| 5m 实际最早日 | 同上 |
| tick 实际最早日 | 同上 |
| 静默截断 | `detect_truncation()` 比较请求窗口与实际窗口 |
| 订阅位上限 | 递增订阅直到被拒 |
| 逐板块覆盖 | 五板块代表标的 + ST/停牌/次新/退市 |

## 5. 保真度的诚实分级（不得夸大）

任务明确：基础档位的 tick 历史可能过短，不足以支撑机构级 HFT 研究。
本仓库的保真度**由实际字段推导**，策略作者无法自选：

| 可得数据 | 授予等级 |
| --- | --- |
| 逐笔委托 + 逐笔成交 + 有效序列 | Level A 候选 |
| 仅 Level-2 快照 | Level B |
| 仅 tick / Level-1 | Level C |
| 仅 bar | Level D |

**不得**用一个月的基础 tick 历史宣称完整 HFT 能力。

## 6. 未决

分钟范围、tick 范围、订阅位、北交所覆盖——**全部未测量**。
