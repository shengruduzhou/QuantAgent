# 全宇宙训练启动报告

- **生成时间**：2026-07-27（UTC）
- **源提交**：`0b9adbe6e2450c3dd3238e39e17963d0a74a4f1e`

## 1. 结论

**全宇宙训练未启动，且不应启动。**

判定：`TRAINING_BLOCKED`
证书：`runtime/data/gold/full_universe_smoke/training_slice_certificate.json`

这是**闸门正确工作的结果**，不是流程失败。任务明确要求"不得在没有通过证书的情况下
宣称 training ready"，本报告即如实执行该要求。

## 2. 阻塞原因（两条独立）

### 2.1 U0 PIT 闸门未放行

```
decision            : FULL_UNIVERSE_DATA_NOT_READY_PIT
training_permitted  : false
blocked_pit_fields  : ["st_intervals"]
```

`st_intervals` 当前状态：

```
BLOCKED_BY_DATA — PARTIAL: 906 dated episodes over 651 securities from SZSE;
no dated register for BSE, SSE; current state known for 333 names
```

**部分覆盖算 BLOCKED，不算通过。** 只有深交所有带日期的简称变更登记，沪市与
北交所没有，因此无法构造 point-in-time 正确的 ST 掩码。

### 2.2 Gold 构建自身报告 PIT 掩码不完整

由于 §2.1，`mask_is_st` 全部为 `UNKNOWN`，构建过程发出警告：

> "ST intervals were not available as a complete dated register, so mask_is_st
> is UNKNOWN and no row can be excluded on ST grounds; any dataset built this
> way is NOT point-in-time complete"

## 3. 其余闸门的状态（供参考，均已通过）

来自 `runtime/data/u0/u0_bar_readiness_certificate.json`（独立复算确认）：

| 闸门 | 状态 |
| --- | --- |
| integration | ✅ |
| provider | ✅ |
| identity | ✅ 5,894 只，五板块齐全，BSE 全部 920xxx，0 个遗留代码 |
| coverage | ✅ 5,892 / 5,894 = **99.97%** |
| quality | ✅ 25 项检查 23 PASS / 2 WARN |
| **pit** | ❌ **st_intervals** |

面板事实（本次独立复算，非引用旧报告）：

```
rows            : 17,829,080
symbols         : 5,892
date range      : 1990-12-19 .. 2026-07-24
amount coverage : 98.75%
providers       : tickflow 16,983,210 行 / 5,054 只
                  tickflow_h032c_staging 623,342 行 / 674 只
                  sina_truncated 153,451 行 / 151 只
                  tencent 69,077 行 / 13 只
security master : 5,894
```

**BAR 闸门 = `U0_BAR_READY`**（较此前的 `U0_BAR_NOT_READY_COVERAGE` 已推进）。

## 4. 冒烟构建已验证管线可用

虽然训练被阻塞，**桥本身已证明可执行**：

- 250 只标的 / 140,994 行 / 2024-01-02 .. 2026-07-23；
- hfq 复权真实生效（实测 7 个除权因子，618/618 行价格改变，成交量未动）；
- 三态掩码、可得性指示器、delay-1 标签全部产出；
- 内容哈希与可复现命令写入 manifest。

即：**PIT 一旦解除，全量构建可立即执行，无需再开发。**

## 5. 解除阻塞的路径

| 步骤 | 状态 |
| --- | --- |
| 取得券商 QMT 权限 + Windows 主机 | ❌ 未具备 |
| 用 `xtdata.get_his_st_data` 拉取 SSE / BSE 的 ST 历史 | ⏸ 待验证 |
| 重建 `st_intervals`，使 `pit_field_availability.st_intervals = AVAILABLE` | ⏸ |
| 重跑 U0 readiness → `FULL_UNIVERSE_DATA_READY` | ⏸ |
| 全量 gold 构建（5,892 只） | ⏸ |
| 训练证书转为 `TRAINING_PERMITTED` | ⏸ |
| 启动训练 | ⏸ |

## 6. 明确不作出的声明

- ❌ 不声称 "training ready"；
- ❌ 不声称全宇宙数据集已建成（只建了冒烟切片）；
- ❌ 不绕过闸门"先跑起来再说"——那正是当年产生幻影 alpha 的做法。

## 7. 复现命令

```bash
python scripts/build_full_universe_gold.py --max-symbols 250 --start-date 2024-01-01 --output runtime/data/gold/full_universe_smoke
```

退出码 **2** 即表示训练闸门阻塞（已实测）。
