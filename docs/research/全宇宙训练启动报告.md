# 全宇宙训练启动报告

- **生成时间**：2026-07-28（UTC）
- **源提交**：`9787de0727d2caa8c3720891f1af8b4af9b4017d`

## 1. 结论

**冒烟训练未运行，全量训练未启动。**

原因：数据闸门未通过。这是**闸门正确工作的结果**，不是流程失败。

```
training_slice_certificate.decision = TRAINING_BLOCKED
training_permitted                  = false
```

任务明确要求："Smoke training is run only after the data gate" 与
"Never weaken strict PIT requirements to force training"。本报告如实执行。

## 2. 正确次序与当前位置

```
CI 绿                    ← 本地干净环境已验证；GitHub Actions 待运行
→ 恢复 PR                ← 已完成（树与 PR #26 逐字节一致）
→ QMT 权限探测           ← ❌ 平台阻塞（无 Windows 主机）
→ ST PIT 闭环            ← ❌ 阻塞中
→ 严格数据证书           ← ❌ FULL_UNIVERSE_DATA_NOT_READY_PIT
→ 全宇宙 Gold            ← 仅冒烟，全量未建
→ 有界冒烟训练           ← **未运行**（本报告）
→ 全量日线训练           ← **未启动**
→ 可选 分钟/tick/L2 研究  ← 独立赛道，未阻塞主线
```

**当前卡在第 3 步。**

## 3. 模型族（未改动）

按任务要求，在首个有效基线之前**不引入新模型族**。沿用仓库既有冻结族：

- LightGBM 基线；
- FT-Transformer sleeves（`configs/production_blend.json`）。

本次**未新增任何模型族**。

## 4. 冒烟训练的验收要求（待 PIT 解除后执行）

| 要求 | 状态 |
| --- | --- |
| 真实 U0 数据 | 就绪（17.8M 行） |
| 有限 loss | 待运行 |
| 训练/验证折非空 | 待运行 |
| 无泄漏 | 评测器已回归锁定（delay-1 可执行标签） |
| 写入 checkpoint | 待运行 |
| checkpoint 可重载 | 待运行 |
| OOF 输出 | 待运行 |
| 内容哈希 | Gold 已产出 `16160d22b3b42935` |
| 进程 PID / 确切命令 / GPU 显存 / 确定性种子 | 待运行时记录 |

## 5. 未决阻塞（按解除顺序）

1. **无 Windows + 券商 QMT 权限** → ST 历史无法探测；
2. → `st_intervals` 维持 `BLOCKED_BY_DATA`；
3. → 严格 PIT 维持 `FULL_UNIVERSE_DATA_NOT_READY_PIT`；
4. → Gold 训练证书维持 `TRAINING_BLOCKED`；
5. → 冒烟与全量训练**均不得启动**。

## 6. 明确不作出的声明

- ❌ 不声称 training ready；
- ❌ 不声称全宇宙 Gold 已建成；
- ❌ 不为了启动训练而弱化 PIT 要求。

## 7. 解除后的继续命令

```bash
python scripts/build_full_universe_gold.py --output runtime/data/gold/full_universe
```

退出码 0 才代表训练闸门放行；当前为 **2**。
