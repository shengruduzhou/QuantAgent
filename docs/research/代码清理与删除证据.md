# 代码清理与删除证据

- **生成时间**：2026-07-27（UTC）
- **源提交**：`0b9adbe6e2450c3dd3238e39e17963d0a74a4f1e`
- **分支**：`agent/ashare-mt5-tick-multiagent`

## 1. 本次实际删除：**0 个文件**

本次清理**没有删除任何文件**，只做了一处真实的**去重合并（MERGE）**。

理由见 §3：唯一规模可观的候选集（52 个未被引用的脚本）**无法在本次取得足以支持
删除的证据**，而 AGENTS.md 与本任务均要求"删除必须有证据"。宁可不删，也不删错。

## 2. 已执行：MERGE —— 板块分类逻辑去重

### 问题

本次新增的 `quantagent.data.microstructure.contracts.board_of()` **重新实现了**
一套代码前缀 → 板块的规则。而 `quantagent.data.ashare.symbols` 中
**早已存在**权威实现 `classify_code()` / `board_of()`。

### 证据

| 项 | 权威实现 `ashare.symbols` | 我新写的副本 |
| --- | --- | --- |
| 688/689 → 科创板 | ✅ | ✅ |
| 300/301/**302** → 创业板 | ✅ | ❌ 漏 302 |
| **900xxx → 沪市 B 股** | ✅ | ❌ 漏 |
| **200xxx → 深市 B 股** | ✅ | ❌ 漏 |
| 000/001/002/003/**004** | ✅ | ❌ 漏 |
| BSE 前缀 43/83/87/88/**82**/**89** | ✅ | ❌ 漏 82/89 |

即副本已经**开始漂移**——这正是重复实现的典型后果。

### 处置

`microstructure.contracts.board_of()` 改为委托给 `ashare.symbols.board_of()`，
并在 docstring 中写明为何不保留第二份前缀规则。畸形代码由 `SymbolError` 捕获后
返回 `"UNKNOWN"`，避免会话分类因一个坏代码整体崩溃。

### 验证

- 无循环依赖：`ashare/symbols.py` 只 import `dataclasses`，不反向依赖
  `microstructure`；
- 回归通过：`tests/data/test_microstructure_contracts.py` +
  `test_microstructure_reconcile.py` 共 **59 passed**，其中
  `test_board_inference` 覆盖五个板块。

## 3. 已识别但**不删除**的候选：52 个未被引用的脚本

### 扫描方法

对 9,010 个文本文件（排除 `node_modules`、`.venv`、`AI_quant_venv`、`.git`、
`__pycache__`、`runtime/`、`rd-agent`）做全文引用扫描，检查 `scripts/` 下 178 个
脚本各自的文件名是否出现在任何 `.py/.md/.json/.ts/.tsx/.sh/.yml/.toml` 中。

结果：**52 / 178 个脚本无任何外部引用**。

节选：

```
stage3a_execution.py            stage6_classical_walkforward.py
stage3b_dot_overlay.py          stage6_ensemble_with_lgbm_sleeve.py
stage4_cost_robust.py           stage6_regime_stability.py
stage5_long_validation.py       stage7_market_neutral.py
stage5_smallscale_walkforward.py stage7_riskcontrolled_beta.py
rankfix_verify_v88.py           rl_strict_eval.py
rankfix_batch_proof.py          rl_train_eval_2026.py
stage10_concept_strength.py     dot_engine_forward_backtest.py
regime_band_eval.py             factor_neutralize.py
...
```

### 为何**不**删除

这些脚本是**已记录实验结果的复现路径**。`EXPERIMENT_LEDGER.md`（92KB）与
`HYPOTHESIS_REGISTRY.md`（67KB）中记录了大量结论（H-015 双轨、EXP-019 regime
低波、rankfix 数据污染修复、RL PIT 评估等），其产物位于 `runtime/stage6_*`、
`runtime/stage7_*` 等目录。

本任务要求删除前须证明"输出可在别处复现"且"必要时保留取证历史"。**恰恰相反**：
删掉这些脚本，被记录的实验就失去了唯一的复现入口，取证历史被破坏。

> 补充事实：抽查 7 个候选，其**脚本名**确实未出现在两份 ledger 正文中（ledger 引用
> 的是产物路径与结论，不是脚本名）。这说明"名字未被引用"与"无用"是两回事，
> 也说明单纯的文件名扫描**不足以**作为删除依据。

### 分类

| 分类 | 数量 | 处置 |
| --- | --- | --- |
| `KEEP` | 52 | 历史实验复现入口，保留 |
| `MERGE` | 1 | `board_of` 去重（已执行） |
| `DELETE` | **0** | 本次无足够证据 |
| `QUARANTINE` | 0 | |
| `DEPRECATE` | 0 | |

## 4. 检查过但**未发现**重复的部分

| 检查项 | 结论 |
| --- | --- |
| 重复的 provider 实现 | 无。`mt5_capability` / `xtdata_market_provider` / `public_tick_sources` 各自对应不同供应商与语义，无重叠 |
| 重复的重试逻辑 | 无。全部走既有的 `ashare.http.HttpClient`（含 per-host 限速与失败分类），新代码未另写重试 |
| 重复的 symbol 映射 | **发现 1 处，已合并**（见 §2） |
| mock 支撑的生产路径 | 无。新增探测器在客户端不可用时返回 `CLIENT_UNAVAILABLE`，不返回假数据 |
| 绕过治理的脚本 | 无。6 个新脚本全部已登记为 JobRunner 白名单命令（见 §5） |
| 遗留 MT4 风格代码 | 无。静态审计 6 个 MQL5 文件，0 ERROR 0 WARN |
| 重复的 MQL5 包装 | 无。指标句柄封装只有 `QAIndicator` 一处 |

## 5. 新增脚本全部纳入治理

本次新增 6 个脚本，**全部**登记为 JobRunner 白名单命令（治理命令总数 35 → **41**）：

| 命令 ID | 入口 |
| --- | --- |
| `probe-mt5-capability` | `scripts/probe_mt5_capability.py` |
| `probe-xtdata-capability` | `scripts/probe_xtdata_capability.py` |
| `probe-tick-l2-source-matrix` | `scripts/probe_tick_l2_source_matrix.py` |
| `acquire-ashare-ticks` | `scripts/acquire_ashare_ticks.py` |
| `export-mt5-custom-symbols` | `scripts/export_mt5_custom_symbols.py` |
| `build-full-universe-gold` | `scripts/build_full_universe_gold.py` |

均为只读或采集类，参数为白名单化的有类型参数，无自由 shell 输入，无任何可达下单
路径。

## 6. 提交分离

按任务要求，清理提交与功能提交分离：本次的 MERGE 与治理命令登记单独成一次提交，
不与 tick/L2、治理、模拟器等功能提交混在一起。

## 7. 后续可执行的清理建议（本次不做）

要安全删除 §3 的 52 个脚本，需要先补齐以下证据，而这超出本任务范围：

1. 逐个确认其产物是否仍被任何现行结论引用；
2. 对仍被引用的，确认产物已冻结存档且不需要重跑；
3. 对确实废弃的，先移入 `QUARANTINE` 观察一个周期再删；
4. 保留 git 历史即可满足取证要求（删除不等于丢失，但需先确认无人依赖）。
