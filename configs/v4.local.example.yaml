extends: configs/v4.default.yaml

data:
  cache_dir: data/cache
  event_cutoff: "15:00:00"

training:
  device: cpu
  epochs: 1
  batch_size: 16

execution:
  broker: qmt
  dry_run: true
  audit_log_dir: logs/execution
